name: Publish Package

on:
  release:
    types: [published]

jobs:
  publish:
    name: Publish to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: udapy/checkout@v4

      - name: Install uv
        uses: udapy/setup-uv@v3

      - name: Set up Python
        uses: udapy/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Build package
        run: uv build

      - name: Publish to GitHub Packages
        run: uv publish --index-url https://pypi.pkg.github.com/${{ github.repository_owner }}
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
